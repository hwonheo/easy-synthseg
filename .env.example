# ── 루트 ──────────────────────────────────────────────────────────
DATA_ROOT=/path/to/fastsurfer-docker/data

# ── 케이스 선택 ────────────────────────────────────────────────────
SID=subjectX

# ── 라이선스 ───────────────────────────────────────────────────────
FS_LICENSE_DIR=/path/to/freesurfer/license/dir
FS_LICENSE_FILE=license.txt

# ── 실행 옵션 ──────────────────────────────────────────────────────
THREADS=4
FORCE_CONVERT=0   # 1이면 dcm2niix 재실행
FORCE=0           # 1이면 synthsr/synthseg/fastsurfer 결과 overwrite

# ── OS 분기 (mac | linux) ─────────────────────────────────────────
# COMPOSE_OS=mac  # 기본값. Linux GPU 서버에서는 linux로 변경
